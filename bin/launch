#!/usr/bin/env bash
set -eu
set -o pipefail

case "$1" in
    tozt|algo)
        "$(dirname "$0")/helpers/launch-$1"
        ;;
    *)
        echo "unknown host $1" >&2
        exit 1
        ;;
esac
